/** Default message notifications for a guild. */
export enum GuildDefaultMessageNotificationLevel {
    /** Members will receive notification for all messages. */
    ALL_MESSAGES = 0,
    /** Members will only receive notifications for messages that @mention them. */
    ONLY_MENTIONS,
}
