/** Display layout for `GUILD_FORUM` channels. */
export enum ForumLayoutType {
    /** No default has been set. */
    NOT_SET = 0,
    /** Display posts as a linear list. */
    LIST_VIEW,
    /** Display posts as a grid of tiles. */
    GALLERY_VIEW,
}
