/** The NSFW level of a guild. */
export enum GuildNSFWLevel {
    /** Default value. */
    DEFAULT = 0,
    /** The guild is deemed explicit. */
    EXPLICIT,
    /** The guild is deemed safe. */
    SAFE,
    /** The guild is age-restricted. */
    AGE_RESTRICTED,
}
