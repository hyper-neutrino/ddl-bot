/** The type of the target of an invite. */
export enum InviteTargetType {
    /** This invite targets a livestream. */
    STREAM = 1,
    /** This invite targets an embedded application (activity). */
    EMBEDDED_APPLICATION,
}
