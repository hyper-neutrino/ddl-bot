/** The premium tier (boost level) of a guild. */
export enum GuildPremiumTier {
    /** The guild has no server boost perks. */
    NONE = 0,
    /** The guild has level 1. */
    TIER_1,
    /** The guild has level 2. */
    TIER_2,
    /** The guild has level 3. */
    TIER_3,
}
