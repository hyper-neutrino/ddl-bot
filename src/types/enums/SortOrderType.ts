/** The sort order of a `GUILD_FORUM` channel's posts. */
export enum SortOrderType {
    /** Sort forum posts by latest activity. */
    LATEST_ACTIVITY = 0,
    /** Sort forum posts by earliest creation date. */
    CREATION_DATE,
}
