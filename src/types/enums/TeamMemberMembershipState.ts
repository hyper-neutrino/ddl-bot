/** The state of a team member's membership on a developer team. */
export enum TeamMemberMembershipState {
    /** The user has been invited. */
    INVITED = 1,
    /** The user has accepted their invitation and is a member. */
    ACCEPTED,
}
