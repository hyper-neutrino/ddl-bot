/** The privacy level of a stage instance. */
export enum StageInstancePrivacyLevel {
    /** This instance is publicly accessible (e.g. on stage discovery). */
    PUBLIC = 1,
    /** This instance is only accessible to guild members. */
    GUILD_ONLY,
}
